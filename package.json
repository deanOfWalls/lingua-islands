{
  "name": "lingua-islands",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "generate-audio": ".venv-audio/bin/python scripts/generate-audio.py || python scripts/generate-audio.py"
  },
  "devDependencies": {
    "typescript": "^5.7.2"
  }
}
